{
  "name": "max-flow-optimizer",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "build:engine": "cmake -S services/engine-cpp -B services/engine-cpp/build -G Ninja -DCMAKE_BUILD_TYPE=Release && cmake --build services/engine-cpp/build -j",
    "dev": "pnpm --parallel --filter @maxflow/api --filter @maxflow/web run dev",
    "dev:full": "pnpm run build:engine && pnpm run dev",
    "build": "pnpm -r --if-present run build && pnpm run build:engine",
    "analytics": "pnpm analytics:generate && pnpm analytics:run && pnpm analytics:aggregate && pnpm analytics:verify && pnpm analytics:report",
    "analytics:setup": "python3 -m venv .venv && .venv/bin/python -m pip install polars==1.14.0 matplotlib==3.9.2 duckdb==1.1.3",
    "analytics:generate": "node ./scripts/analytics-generate.mjs",
    "analytics:run": "node ./scripts/analytics-run.mjs",
    "analytics:aggregate": "node ./scripts/analytics-aggregate.mjs",
    "analytics:report": "node ./scripts/analytics-report.mjs",
    "analytics:compare": "node ./scripts/analytics-compare.mjs",
    "analytics:verify": "node ./scripts/analytics-verify.mjs",
    "analytics:profile": "node ./scripts/analytics-profile.mjs",
    "analytics:small": "node ./scripts/analytics-profile.mjs small",
    "analytics:50k": "node ./scripts/analytics-profile.mjs 50k",
    "analytics:500k": "node ./scripts/analytics-profile.mjs 500k",
    "analytics:benchmark": "node ./scripts/analytics-profile.mjs benchmark",
    "analytics:tune": "node ./scripts/analytics-tune.mjs",
    "analytics:timed": "node ./scripts/analytics-timed.mjs",
    "smoke:api": "node ./scripts/smoke-api.mjs",
    "benchmark:api": "node ./scripts/benchmark-api.mjs",
    "test:engine": "pnpm run build:engine && ctest --test-dir services/engine-cpp/build --output-on-failure",
    "test": "pnpm -r --if-present run test && pnpm run build:engine && ctest --test-dir services/engine-cpp/build --output-on-failure",
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck"
  }
}
